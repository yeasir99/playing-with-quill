import React from 'react';

function FeatureContainer({ heading, paragraph }) {
  return (
    <div className="App">
      <h>{heading}</h>
      <p>{paragraph}</p>
    </div>
  );
}

export default FeatureContainer;
